{
  "name": "ad-manager-platform",
  "version": "0.3.1",
  "description": "Ad management platform for automating posting, scheduling, and analytics",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "dependencies": {
    "html-pdf-node": "^1.0.8",
    "pdfkit": "^0.13.0",
    "resend": "^3.0.0"
  },
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  }
}
